import Vue from 'vue'
import Vuex from 'vuex'
import channels from './channels.js'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
      channels
  }
})
